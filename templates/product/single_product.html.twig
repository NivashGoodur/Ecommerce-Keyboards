<div class="product-item text-center">
    <a href="{{ path('product', {slug: product.slug}) }}"><img src="/uploads/{{ product.illustration }}" class="img-fluid" alt="{{ product.name }}"></a>

    <h4>{{ product.name }}</h4>
    <p>{{ product.subtitle }}</p>
    <p class="font-weight-bold">{{ (product.price /100)|number_format(2, ',', '.')}}
        €</p>
</div>